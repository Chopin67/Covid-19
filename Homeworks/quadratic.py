# azh5kn Alexandra Hickman

def big_root(a, b, c):
    # going to give the bigger root of the quadratic equation

    return (-b + (b**2 - 4*a*c)**(1/2))/(2*a)



def small_root(a, b, c):
    # going to give the smaller root of the quadratic equation

    return (-b - (b**2 - 4*a*c)**(1/2))/(2*a)
